# Environment Configuration for Touchless Photobooth

# URL of the deployed Google Apps Script Webhook (from Code.gs)
# Used for auto-uploading final composite photos to Google Drive.
# Must be accessible from the browser (CORS configured in Code.gs).
NEXT_PUBLIC_GOOGLE_SCRIPT_URL="https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"
